<span {{ $attributes->class('flex items-center gap-2') }}>
    <x-app-logo-icon class="size-8" />
    <span class="font-serif text-lg font-semibold lowercase leading-none">{{ config('app.name', 'Kritee') }}</span>
</span>
